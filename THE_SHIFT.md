# The Shift

From:

**observe → infer → predict**

to:

**observe → infer → predict → intervene → state changes → observe again → learn → predict again**

The difference is causal.

The model is no longer external to the system it models.

**The intervention changes the system being modeled.**

That creates a different intelligence problem: reasoning over state while accounting for the system's own influence on that state.
